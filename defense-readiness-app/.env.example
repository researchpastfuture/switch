# Only needed to run the one-time AI drafting script (`npm run draft`).
# Get a key at https://console.anthropic.com/  ->  paste it below, then run:
#   cp .env.example .env
#   npm run draft
# After drafting once, this key is NOT needed to view, build, or deploy the site.
ANTHROPIC_API_KEY=
